# LICENSE.md

本发布包包含两部分：

## 1. 插件部分（plugin/）

本项目为 MIT License，声明为插件作者自行整理的发布内容。

## 2. 宿主补丁（host-patch.diff）

`host-patch.diff` 是 `deepseek-harness` 工作区的源代码改动记录。上游项目的许可和版权归属随其仓库根目录 LICENSE 文件为准。本发布包不重新分发上游公共源码，只分发补丁片段，因此保留补丁行为，不改变上游代码版权。

## 3. 用户责任

使用前请确认 `deepseek-harness` 的对应开源许可证与你的使用场景一致。本文件夹不含上游完整源文件，实际构建仍基于用户自己安装的 `deepseek-harness`。
